/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn2.thecatapi.com', 'cdn2.thedogapi.com', 'random.imagecdn.app'],
      },
};

export default nextConfig;
